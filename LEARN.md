# LEARN.md

## 1. Что сделали
- Написали скрипт `file_sorter.py` на Python для автоматической сортировки файлов в папке "Загрузки"
- Использовали модули `os` (работа с файловой системой) и `shutil` (перемещение файлов)
- Добавили мониторинг папки в реальном времени через `watchdog`

## 2. Что разобрать
- Как `os.path` обрабатывает пути в разных ОС
- Разницу между `shutil.move()` и `os.rename()`
- Принцип работы `FileSystemEventHandler` из watchdog
- Обработку исключений при работе с файлами

## 3. Ссылки на доку
- [os.path — Python docs](https://docs.python.org/3/library/os.path.html)
- [shutil — Python docs](https://docs.python.org/3/library/shutil.html)
- [watchdog на PyPI](https://pypi.org/project/watchdog/)

## 4. Вопросы для самопроверки
1. Как получить список файлов в папке через `os`?
2. Чем отличается абсолютный путь от относительного?
3. Как безопасно создать папку, если она не существует?
4. Какие расширения файлов нужно обрабатывать особым образом?
5. Как остановить бесконечный цикл мониторинга?
6. Какие права нужны скрипту для работы с файлами?
7. Как добавить логгирование операций?
